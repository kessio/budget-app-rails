<nav class="navbar navbar-expand-md navbar-dark bg-primary fixed-top">
 <button class="navbar-toggler" type="button">
    <span class="navbar-toggler-icon"></span>
  </button>
  <h3 class="text-white p-3">Categories</h3>
  <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-white fs-4 fw-bold text-decoration-none p-3" %>
</nav>


<p style="color: green"><%= notice %></p>
<div class="container mt-5">
<div id="groups">
  <% @groups.each do |group| %>
    <%= render group %>
  <% end %>
</div>
<div class="bg-success fixed-bottom p-2">
<div class="border p-3 text-center fw-bolder"><%= link_to "Add New Category", new_group_path, class: "text-white text-decoration-none"%></div>
</div>


</div>
