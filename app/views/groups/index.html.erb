<nav class="navbar navbar-expand-md navbar-dark bg-primary fixed-top">
 <button class="navbar-toggler" type="button">
    <span class="navbar-toggler-icon"></span>
  </button>
  <h3 class="text-white p-3">Categories</h3>
  <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-white fs-4 fw-bold text-decoration-none p-3" %>
</nav>


<p style="color: green"><%= notice %></p>
<div class="col-12 bg-white py-4 shadow-sm rounded">
<span>Total:</span><span>$500</span>
</div>
<div class="container mt-5">
<div id="groups">
  <% @groups.each do |group| %>
    <%= render group %>
    <p>
      <%= link_to "Show this group", group_expenses_path(group.id) %>
    </p>
  <% end %>
</div>

<%= link_to "New group", new_group_path %>

</div>
