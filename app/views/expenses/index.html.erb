<nav class="navbar navbar-expand-md navbar-dark bg-primary fixed-top">
  <%= link_to image_tag('back.png', alt: "back-arrow"), groups_path,  class: "px-2"%>
  <h3 class="text-white p-3">Expenses</h3>
  <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-white fw-bold text-decoration-none p-3" %>
</nav>

<p style="color: green"><%= notice %></p>
<div class="col-12 bg-white py-4 shadow-sm rounded">
<span>Total:</span><span>$500</span>
</div>
<div class="container mt-5">
<div id="expense">
  <% @expenses.each do |expense| %>
  <div class="col-12 d-flex justify-content-between bg-white py-4 px-2 mt-3">
   <div class="text-black-50 fw-bolder">
    <%= expense.name %>
    </div>
  
    <div class="fw-bolder">
    $<%= expense.amount %>
  </div>
    </div>
  <% end %>
</div>
</div>

<div class="bg-success fixed-bottom p-2">
<div class="border p-3 text-center fw-bolder"><%= link_to "Add New Expense", new_group_expense_path, class: "text-white text-decoration-none"%></div>
</div>
