<p style="color: green"><%= notice %></p>

<h1>Expenses</h1>

<div id="expenses">
  <% @group_expenses.each do |group_expense| %>
    <%= render expense %>
    <p>
      <%= link_to "Show this expense", group_expense %>
    </p>
  <% end %>
</div>

<%= link_to "New expense", new_group_expense_path %>
